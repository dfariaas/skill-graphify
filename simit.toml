[flake]
mode = "custom"
scope = "hooks-only"
components = ["treefmt", "nix-flake-check", "uv-ruff-format"]

[flake.expected_outputs]
checks = ["pytest"]

[ci]
runtime = "nix"
runner = "ubuntu-latest"
components = ["flake-wiring", "flake-evaluation", "checks"]
